// import React from 'react';
// import * as S from '../styles';
// import { useUserProfile } from '../../hooks/customs/useUser';

// const UserIntro = () => {
//   const { profile } = useUserProfile();
//   return (profile?.intro && <div>{profile?.intro}</div>) || <S.EmptyContent>소개글이 없습니다.</S.EmptyContent>;
// };

// export default UserIntro;
export default {};
